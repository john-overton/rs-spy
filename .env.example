# Free Alpaca paper-trading account (no funding required for data access):
# https://alpaca.markets/ -> Generate API Keys
ALPACA_API_KEY_ID=
ALPACA_API_SECRET_KEY=

# Postgres runs-store (see docker-compose.yml). Defaults to the compose
# credentials on host port 55432; only set this to point at a different host/db.
# DATABASE_URL=postgresql://rs_spy:rs_spy@localhost:55432/rs_spy
